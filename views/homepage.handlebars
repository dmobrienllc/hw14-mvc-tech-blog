<!-- Loop over each object in the projects array -->
{{#each posts as |post| }}
<div class="row mb-4 project">
  <div class="col-md-5">
    <h2>
      {{{get_emoji}}}
      <a href="/post/{{post.id}}">{{post.post_title}}</a>
    </h2>
    <p>
      <span for="img" aria-label="post-body">💲</span>
      <span class="post-body">{{post.post_body}}</span>
      needed!
    </p>
    {{!-- <p>Created by {{post.user.name}} on {{format_date project.date_created}}</p> --}}
    <p>Created by {{post.user.user_name}}</p>
  </div>
  {{!-- <div class="col-md-7">
    <p>
      {{project.description}}
    </p>
  </div> --}}
</div>
{{/each}}
